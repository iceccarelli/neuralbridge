import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  reactStrictMode: true,
  // You can add more config here later if needed (e.g. images, redirects)
};

export default nextConfig;
